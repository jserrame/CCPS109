def three_summers(items, goal):
    for first in range(len(items)):
        for second in range((first+1), len(items)):
            for third in range((second)+1, len(items)):
                sum = int(items[first]) + int(items[second]) + int(items[third])
                if int(sum) == int(goal):
                    return True
    return False
